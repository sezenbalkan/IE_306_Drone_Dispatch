env_config: configs/eval_standard.yaml
seed: 0
total_steps: 6000000
max_steps_per_episode: 1000
buffer_size: 100000
batch_size: 128
gamma: 0.99
learning_rate: 0.0001
reward_scale: 10.0
normalize_time: true
hidden_sizes: [256, 256]
learning_starts: 1000
train_every: 4
target_update_every: 1000
epsilon_start: 1.0
epsilon_end: 0.05
epsilon_decay_steps: 4000000
double_dqn: false
dueling: false
checkpoint_interval: 500000
print_every_episodes: 500
log_path: logs/dqn_6m_seed0.csv
weight_path: weights/dqn_6m_seed0.pt
